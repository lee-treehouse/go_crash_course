package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Println(math.Floor(2.7))
	fmt.Println(math.Ceil(2.7))
	fmt.Println(math.Sqrt(16))
	// this example is incomplete, I couldn't get the module system working.. come back to it later
}
